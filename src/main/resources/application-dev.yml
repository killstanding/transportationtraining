debug: true
logging:
  level:
    root: debug
    tech:
      wetech:
        admin:
          mapper: trace
server:
  port: 8080
  servlet:
    context-path: /wetech-admin
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    druid:
      initial-size: 1
      max-active: 20
      min-idle: 1
      stat-view-servlet:
        allow: true
      test-on-borrow: true
    password: 
    url: jdbc:mysql://localhost:3306/wetech_admin?useUnicode=true&characterEncoding=utf-8
    username: root
  thymeleaf:
    cache: false